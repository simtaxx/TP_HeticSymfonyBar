<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">The Simple Bar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link  {% if routeName is defined and routeName == '/home' %} active {% endif %}" href="{{ path('home') }}">Home <span class="sr-only">(current)</span></a>
            </li>
            {% for category in categories %}
            <li class="nav-item">
                <a class="nav-link {% if routeName is defined and routeName == '/categories/' ~ category.name %} active {% endif %}" href={{ path('categories', {'category' : category.name }) }}>{{ category.name }}</a>
            </li>
            {% endfor %}
            <li class="nav-item">
                <a class="nav-link {% if routeName is defined and routeName == '/mentions' %} active {% endif %}" href="{{ path('mentions') }}">Mentions</a>
            </li>
        </ul>
    </div>
</nav>